{% extends 'admin/adminBase.html.twig' %}

{% block title %}Ajouter une Remise{% endblock %}

{% block body %}

<section class="list">
    <div class="form-container">
    <h1>Ajouter une Remise</h1>
       {{ form_start(form) }}
        <div class="form-row">
        {{ form_widget(form) }}
        </div>
        <button type="submit" class="button button-submit">Envoyer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
